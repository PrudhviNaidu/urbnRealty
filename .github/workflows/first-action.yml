name: my-first-action

on:
  push

jobs:
  my-first-action:
    runs-on: ubuntu-latest
    steps:
      run: echo "Hello, world!"

  say-hello:
      runs-on: ubuntu-latest
      steps:
        run: echo "Hello, ${{ github.actor }}!"
        shell: python